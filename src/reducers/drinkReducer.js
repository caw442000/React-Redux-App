const initialState = {
  drinks: [],
  isFetchingData: false,
  error: ""
};


export const drinkReducer = (state = initialState, action) => {
  switch(action.type) {

    default:
      return state;
  }  


}
